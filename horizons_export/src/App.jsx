import React,{useEffect,useState} from 'react';
import {Routes,Route,useLocation,Link} from 'react-router-dom';
import Header from '@/components/Header';import Footer from '@/components/Footer';import HomePage from '@/pages/HomePage';import ProjectDetail from '@/pages/ProjectDetail';import InterestDetail from '@/pages/InterestDetail';import Contact from '@/components/Contact';
export default function App(){const [language,setLanguage]=useState(()=>{try{return localStorage.getItem('portfolio-language')==='zh'?'zh':'en'}catch{return 'en'}});const location=useLocation();useEffect(()=>{document.documentElement.lang=language==='zh'?'zh-CN':'en';try{localStorage.setItem('portfolio-language',language)}catch{}},[language]);useEffect(()=>{if(location.hash){requestAnimationFrame(()=>document.getElementById(location.hash.slice(1))?.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'}))}else window.scrollTo(0,0)},[location.pathname,location.hash]);return <><a className="skip-link" href="#main">{language==='zh'?'跳转到正文':'Skip to content'}</a><Header language={language} setLanguage={setLanguage}/><Routes><Route path="/" element={<HomePage language={language}/>}/><Route path="/projects/:id" element={<ProjectDetail language={language}/>}/><Route path="/interests/:id" element={<InterestDetail language={language}/>}/><Route path="*" element={<main id="main" className="not-found shell"><h1>404</h1><p>{language==='zh'?'页面不存在。':'This page could not be found.'}</p><Link to="/">{language==='zh'?'返回首页':'Return home'}</Link></main>}/></Routes><Contact language={language}/><Footer/></>}
